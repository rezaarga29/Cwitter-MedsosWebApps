<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile Cards</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }

    .navbar {
      background-color: #007bff;
      padding: 10px 20px;
    }

    .navbar-brand {
      color: #fff;
      font-size: 24px;
      margin-right: 20px;
      text-decoration: none;
    }

    .search-input {
      padding: 8px 15px;
      border: none;
      border-radius: 5px;
      margin-right: 10px;
      width: 200px;
    }

    .search-btn {
      padding: 8px 15px;
      background-color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .container {
      max-width: 800px;
      margin: 20px auto;
      /* Penambahan margin di sini */
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
    }

    .card {
      width: 300px;
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      margin: 10px;
      /* Penambahan margin di sini */
      overflow: hidden;
    }

    .card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .card-content {
      padding: 20px;
    }

    .name {
      font-size: 24px;
      color: #333;
      margin-bottom: 10px;
    }

    .address {
      font-size: 18px;
      color: #666;
      margin-bottom: 20px;
    }
  </style>
</head>

<body>
  <nav class="navbar">
    <a href="#" class="navbar-brand">Cwitter</a>
    <form action="#" method="GET">
      <input type="text" name="search" class="search-input" placeholder="Search...">
      <button type="submit" class="search-btn">Search</button>
    </form>
  </nav>
  <div class="container">
    <% data.forEach((e)=> { %>
        <div class="card">
          <img src="<%= e.Profile.photoProfile %>" alt="Profile Photo">
          <div class="card-content">
            <h2 class="name"><%= e.Profile.name %></h2>
            <p class="address"><%= e.Profile.address %></p>
            <p class="address"><%= e.email %></p>
            <p class="address"><%= e.Profile.userName %></p>
          </div>
        </div>
    <% }) %>
  </div>
</body>

</html>